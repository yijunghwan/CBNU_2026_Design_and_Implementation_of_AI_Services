"""agent2: 유지보수형 파이프라인 기반 청년정책 에이전트 (재작성 버전).

설계 원칙
- 각 단계(Stage)는 단일 책임을 가지며 PipelineContext만 입출력으로 사용한다.
- 안전(민감정보)/제어(동의/취소) 등 극히 일부만 규칙 기반이고, 의도/툴계획은 LLM이 자율 결정한다.
- 질문에서 추출한 슬롯은 맵핑 테이블로 표준값으로만 정규화해 검색 오염을 막는다.
"""
